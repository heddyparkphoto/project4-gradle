apply plugin: 'java'

/* Lesson video sdk 1.7 is the latest sdk that supports multi-module Gradle at this time */
sourceCompatibility = 1.7

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
}

